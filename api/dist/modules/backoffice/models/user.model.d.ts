export declare class User {
    username: string;
    password: string;
    roles: string[];
    active: boolean;
    constructor(username: string, password: string, roles: string[], active: boolean);
}
